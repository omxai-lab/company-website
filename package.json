{
  "name": "omxai-monorepo",
  "version": "1.0.0",
  "description": "OMXAI Corporate Website and Product Portal Monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "packageManager": "pnpm@10.30.3",
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
